spring.application.name=TrainingManagementSystemApplication
server.port=8081

# Database Configuration (override via env vars in production)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/TranningManagementSystem?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:swapnil@182}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.base-url=http://localhost:8081
file.upload-dir=uploads
spring.web.resources.static-locations=file:uploads/,classpath:/static/

# JWT Configuration (override via env vars in production)
app.jwt.secret=${JWT_SECRET:eduflow_secret_key_for_jwt_authentication_system_eduflow_lms_2026_super_secure_key}
app.jwt.expiration-ms=${JWT_EXPIRATION:86400000}

# Gmail SMTP Configuration (override via env vars in production)
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:datturathod2333@gmail.com}
spring.mail.password=${SMTP_PASSWORD:wquu bfza nfan wmzd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Default Admin Configuration (override via env vars in production)
app.admin.email=${ADMIN_EMAIL:admin@gmail.com}
app.admin.password=${ADMIN_PASSWORD:admin@123}

# CORS Configuration
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Logging - production safe
logging.level.root=INFO
logging.level.com.training=INFO
logging.level.org.springframework.security=WARN